-r requirements-mujoco.txt
# Adapter inspects constituent feedback packets to reject partial/stale state.
# AgileX publishes pyAgxArm on GitHub rather than the Python package index.
# Pin private packet access and the project-local acceleration compatibility fix.
pyAgxArm @ git+https://github.com/agilexrobotics/pyAgxArm.git@e7aef17d54cac80cbaeb1b4110ab3d8f1337a95b
